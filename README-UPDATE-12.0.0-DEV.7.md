# Update 12.0.0 DEV.7 installieren

1. ZIP entpacken.
2. Den enthaltenen Ordner `public` in den Projektordner kopieren.
3. Vorhandene Dateien ersetzen.
4. In GitHub Desktop committen und pushen.

Commit-Vorschlag:

`Version 12.0.0 DEV.7 – Foto-Upload-Teaser finalisiert`

Es werden keine Dateien aus `public/content/` mitgeliefert oder überschrieben.
